{
  "name": "gen-image-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker API proxy for AI image generation",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production"
  },
  "keywords": ["cloudflare", "worker", "ai", "image-generation", "cors"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^3.94.0",
    "typescript": "^5.7.2"
  }
}
