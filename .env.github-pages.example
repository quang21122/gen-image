# GitHub Pages Production Environment Variables
# Copy this file to .env.production for local testing of GitHub Pages build

# GitHub Pages deployment flag
VITE_GITHUB_PAGES=true

# Cloudflare Worker Proxy Configuration (RECOMMENDED)
# URL of your deployed Cloudflare Worker that handles CORS and API proxying
# This is the SECURE way to deploy to GitHub Pages without exposing API credentials
# Example: https://gen-image-worker.your-subdomain.workers.dev
VITE_WORKER_PROXY_URL=https://your-worker.workers.dev

# Legacy Direct API Configuration (NOT RECOMMENDED for production)
# Only use these if you can't deploy a Cloudflare Worker
# These credentials will be exposed in the client bundle
# VITE_CLOUDFLARE_ACCOUNT_ID=your_account_id_here
# VITE_CLOUDFLARE_API_TOKEN=your_api_token_here
# VITE_API_BASE_URL=https://api.cloudflare.com/client/v4
