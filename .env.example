# Server-side Cloudflare API Configuration (SECURE - not exposed to browser)
# Get these values from your Cloudflare dashboard:
# 1. Account ID: Go to the right sidebar of any page in the Cloudflare dashboard
# 2. API Token: Go to "My Profile" > "API Tokens" > "Create Token" with AI permissions
CLOUDFLARE_ACCOUNT_ID=your_account_id_here
CLOUDFLARE_API_TOKEN=your_api_token_here

# Cloudflare Worker Proxy Configuration (RECOMMENDED for production)
# URL of your deployed Cloudflare Worker that handles CORS and API proxying
# Example: https://gen-image-worker.your-subdomain.workers.dev
VITE_WORKER_PROXY_URL=https://your-worker.workers.dev

# Legacy Client-side Configuration (DEPRECATED - for development fallback only)
# These are only used in development mode when no Worker proxy is available
# In production, use the Worker proxy instead for security
VITE_CLOUDFLARE_ACCOUNT_ID=your_account_id_here
VITE_CLOUDFLARE_API_TOKEN=your_api_token_here

# Development Configuration
# Base URL for Cloudflare API (usually doesn't need to be changed)
VITE_API_BASE_URL=https://api.cloudflare.com/client/v4
